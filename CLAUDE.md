# CLAUDE.md — Fe

مخطّط تفاعلي ثلاثي الأبعاد لأنماط فشل المعادن والسبائك — ثماني عشرة سبيكة من سبع عائلات. اقرأ `README.md` أوّلًا.

## القواعد غير القابلة للتفاوض

1. **لا ملفّات نماذج ثلاثية الأبعاد.** لا `.glb` ولا `.gltf` ولا `.obj`. كل الهندسة مُولَّدة برمجيًّا.
2. **لا تخزين في المتصفّح.** لا `localStorage` ولا `sessionStorage` ولا `IndexedDB` ولا كوكيز.
3. **لا موارد شبكة وقت التشغيل.** لا خطوط من CDN ولا خرائط HDR. البيئة مُولَّدة من `RoomEnvironment`.
4. **مدفوع بالبيانات.** نمط فشل جديد = صفّ في `src/data/modes.ts` ودالة في `src/engine/failure.ts`.
   سبيكة جديدة = صفّ في `src/data/alloys.ts` لا غير؛ إن كان نمط لا ينطبق عليها فاذكره في
   `notApplicable` مع سببه بدل تلفيق فيزياء له. إن وجدت نفسك تكتب `Torsion.tsx` فقف — المكان صفّ في الجدول.
5. **كل الأبعاد بالمليمتر** في `src/data/`. التحويل مرّة واحدة عند حدّ العرض بـ `SCENE_SCALE`.
   لا تكتب بُعدًا داخل مكوّن.
6. **لا رقم مرسوم بالعين.** كل ثابت في `src/engine/` إمّا علاقة منشورة أو ملاءمة مذكور مرجعها في
   تعليق فوقها. أضِف توكيدًا في `scripts/verify-engine.ts` لكل علاقة جديدة.
7. **يعمل باللمس.** الجهاز المستهدف iPad Pro. تحقّق عند 1024×1366 و1366×1024. أهداف اللمس ≥ 44 px.

## قاعدة اللغة

المصطلحات التقنية وأسماء الحقول ورموز الوحدات تبقى **بالإنجليزية كما تكتبها المعايير**.
الشرح **بالعربية الفصحى**. لا تترجم أبدًا: `Necking`, `Cup and Cone`, `Beach Marks`, `PREN`, `CPT`,
`DBTT`, `Weld Decay`, `Paris`, `Larson-Miller`, `von Mises`, `Considère`, `Charpy`, `NACE`.

اللوحات RTL. اللوحة ثلاثية الأبعاد تبقى LTR.

## قبل كل دفع

```
npm run verify:engine    # الفيزياء مقابل القيم المنشورة
npm run build            # فحص الأنواع + بناء الإنتاج
npm run verify:view      # تصريف الـ shaders وكل نمط في متصفّح حقيقي
```

`verify:view` هو الفحص الذي لا يغني عنه غيره: مدقّق الأنواع لا يرى خطأ في GLSL،
والـ shader المعطوب يظهر كجسم أسود لا كخطأ بناء.

## مزالق معروفة

- **كلمات GLSL المحجوزة.** `active` و`sample` و`input` محجوزة في GLSL ES. سمِّ بغيرها.
- **التمويه عبر الأنماط.** تبديل النمط قطعٌ لا انتقال. `Specimen.tsx` يُثبِّت اللقطة عند التبديل،
  وإلّا ظهرت هندسة النمط السابق عند معدّل إطارات منخفض.
- **العمود الدائري المَلْويّ لا تتغيّر صورته الظلّية.** الشبكة المحفورة على السطح هي ما يُظهر اللَّي —
  لا تحذفها.
- **المعدن اللمّاع يبتلع خريطة الإجهاد.** `metalnessFactor` يهبط حيث تشتدّ الخريطة عمدًا.
- **React لا يرى `el.value = x`.** فحص المتصفّح يمرّر القيمة عبر الـ native setter.
